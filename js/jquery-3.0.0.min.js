 var str = "";
    var cost = 0;
    $(document).ready(function () {
     
	 

      $("#first_form").submit(function (e) {
        e.preventDefault();
        var user_name = $("#user_name").val();
        var email = $("#email").val();
		var number = $("#number").val();
        $(".error").remove();
        if (user_name.length < 1) {
          $("#user_name").after('<span class="error">This field is required</span>');
        }
        if (email.length < 20) {
          $("#email").after('<span class="error">This field is required</span>');
        } else {
			var regEx =/^[A-Z0-9] [A-Z0-9.%+-]{0,63}@(?:[A-Z0-9-]{1,63}\.) {1,125}[A-Z]{2,63}$/;
			var validEmail = regEx.text(email);
			if(!validEmail) {
				$('#email').after('>span class="error">enter a valid email</span>');
			}
		
		}
		 if (number.length < 11) {
          $("#number").after('<span class="error">This field is required</span>');
        }
		function submitAlert(){
			alert("thank you")
		}
	 submit(function(e){
			alert("thank you for you order, will get back to you shortly");
		});
      });
    });

    function CalculateTotal() {
      //var amount = $("#amount").val();  //using jquery to get input value, same as the below
      var amount = document.querySelector("#amount").value; //using normal js  and Using querySelector because of the complex rules instead  of
      var MexTexGood = $("#MexTexGood").val(); //getElementById
      if (`${str}` === `select`) {
        alert("Please choose Burger");
      } else {
        if (amount == "") {
          alert("How many Burgers?");
        } else if (amount < 0) {
          alert("Negative value not accepted in How many Burgers filed");
        } else {
          if (MexTexGood != "") {
            const percentageValueOfTen = cost * amount;
            var total = (percentageValue / 100) * 10000;
            document.querySelector("#total").value = `${total}`;
          } else {
            const total = cost * amount;
            document.querySelector("#total").value = `${total}`;
          }
        }
      }
    }


	 
 